# .woodpecker.yml
pipeline:
  build:
    image: node:12
    commands:
      - USER root
      - tree
      - cp template.build .env-cmdrc
      - ls -la
      - npm install --legacy-peer-deps
      - yarn build:local
    when:
      branch: develop