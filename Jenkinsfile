pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                sh '''#!/bin/bash
                 echo "hello world" 
                '''
                
            }
        }
    }
}