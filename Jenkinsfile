pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                sh '''#!/bin/bash
                    cd /srv
                    ls -la
                '''
                
            }
        }
    }
}