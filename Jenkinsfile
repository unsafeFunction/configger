pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                sh '''#!/bin/bash
                 ls -la
                '''
                
            }
        }
    }
}